A All-one-Matrcices
链接：https://ac.nowcoder.com/acm/contest/888/A
来源：牛客网

题意：给出一个01矩阵，求不被其他矩阵包含的矩阵有多少个全1子矩阵
#include <stdio.h>
#include <stack>
using namespace std;
char s[3005][3005];
int add[3005][3005];
int up[3005];
  
int main() {
    int n, m;
    int ans = 0;
    scanf("%d%d", &n, &m);
    for (int i = 0; i < n; i++) {
        scanf("%s", s[i]);
    }
    for(int i = 0; i < n; i++) 
	{
        for(int j = 0; j < m; j++) 
		{
            if(s[i][j] == '0')
                up[j] = 0;//记录每一行的高度，直方图的高度 
            else
                up[j]++;
        }
        stack<int> S;
        for(int j = 0; j <= m; j++) {
            while(!S.empty() && up[S.top()] > up[j]) {
                int t = S.top();
                S.pop();
                int l;
                while(!S.empty() && up[S.top()] == up[t]) {
                    S.pop();
                }
                if(S.empty()) l = 0;
                else l = S.top() + 1;
                if(i == 0)  ans++;
                else if(add[l][j - 1] != i) ans++;
                 add[l][j - 1] = i + 1;
            }
            S.push(j);
        }
    }
    printf("%d\n", ans);
}

/*****************************************************************************************************************************************/
B Beauty Values
链接：https://ac.nowcoder.com/acm/contest/888/B
来源：牛客网

题意：给你一个数组，求子序列中的元素个数和子序列个数的乘积之和
题解：一个微妙的题目
考虑在数组中插入后一个数字对答案的影响，如果前面的数字没有重复，那么就是ans+=i*(i+1)/2
如果前面有重复出现过的数字，那么久减去对应的那一部分的ans，所有出现过的数字可以用一个cur来计数
如果说不存在与之前的相重复那么这个时候的cur=0，所以可以统一成为一种答案
#include<iostream>
#include<cstring>
using namespace std;
typedef long long ll;
const int maxn=1e5+9;
ll cnt[maxn],a[maxn];

int main() 
{

    ll N;
    cin>>N;
	ll ans=0;
	ll cur=0;
	for(ll i=1;i<=N;i++)
    {
    	cin>>a[i];}
    for(ll i=1;i<=N;i++)
    {
    	cur+=cnt[a[i]];
		ans+=(i*(i+1))/2-cur;
		cnt[a[i]]=i;
		
	}
    printf("%lld",ans);
}


/*****************************************************************************************************************************************/
C
/*****************************************************************************************************************************************/
D
/*****************************************************************************************************************************************/
E
/*****************************************************************************************************************************************/
F
/*****************************************************************************************************************************************/
G
/*****************************************************************************************************************************************/
H
/*****************************************************************************************************************************************/
I
/*****************************************************************************************************************************************/
J
/*****************************************************************************************************************************************/
